import {spawn} from "node:child_process";import {mkdir} from "node:fs/promises";import path from "node:path";import {fileURLToPath} from "node:url";import ffmpegPath from "ffmpeg-static";const root=path.dirname(fileURLToPath(import.meta.url));const out=path.join(root,"assets","generated");await mkdir(out,{recursive:true});for(let i=1;i<=10;i++){const id=String(i).padStart(2,"0");await new Promise((resolve,reject)=>{const p=spawn(ffmpegPath,["-y","-hide_banner","-loglevel","error","-i",path.join(root,"assets","photos","reportage-"+id+".jpg"),"-frames:v","1","-c:v","libwebp","-q:v","86","-compression_level","5",path.join(out,"reportage-"+id+".webp")],{stdio:"inherit"});p.on("error",reject);p.on("exit",c=>c===0?resolve():reject(new Error("photo optimize failed "+id)))})}
